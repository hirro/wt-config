# Property used on app startup to check the config server status
configserver:
    name: Worktajm Registry config server
    status: Connected to the Worktajm Registry config server, using https://github.com/hirro/wt-config !

test:
  a: 1
